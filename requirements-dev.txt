-r requirements.txt

pytest==8.3.3
pytest-cov==5.0.0
pyinstaller==6.10.0
ruff==0.6.9

# Generación del icono .ico desde icon.svg (scripts/build_icon.py).
# PyMuPDF rasteriza SVG con gradientes vía MuPDF; Pillow ensambla el .ico multi-tamaño.
# Ambos son wheels puros en Windows — sin dependencias del sistema.
PyMuPDF==1.24.10
Pillow==10.4.0
